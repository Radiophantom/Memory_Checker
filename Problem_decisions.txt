1.
Бит запуска теста сам сбрасывается пересинхронизированным сигналом регистром 1( slave.busy ).
Выделяем фронт события запуска (исключаем вероятность ложного срабатывания короткого теста, пока бит запуска не обнулился.
Когда busy опускается нужно читать регистр 2, там информация о результате теста, если ошибка в нем адрес ячейки с ошибкой.
Перед запуском теста надо определить все регистры.
Для каждого регистра пересинхронизация на приемной стороне.
При одновременном ошибочном запросе к регистру запуска, приоритет у сброса. Повторный запуск теста во время его выполнения не даст результата, так как не будет фронта.
2.
Мерить в хв-тиках время от первого запроса до последнего и по этому числу передавать в процессор, на нем считать по формуле (хв-тик*период м/у памятью и модулем) - результат средняя прпоускная способность за сессию, если интересна пиковая, то можно маленькими берстами отправлять, чтобы исключить притормаживание аппаратным контроллером, зависит от типа памяти, если больше средняя - то максимально длинный берст, чтобы статистически это было ближе к среднему.
3.
Первое измерение проводится от момента отправки команды чтения и до первого фронта сигнала валидности, дальше, так как только берст, мерить паузы между ответами. НАДО ОБДУМАТЬ ВАРИАНТ ОДИНОЧНЫХ ДОСТУПОВ.
4.
Хранение начального состояния РСЛОС на паттерн данных и восстанавливать данные в том же модуле, что и генерятся, чтобы не тратить лишние ресурсы, так как берсты могут быть длинные, до 1024 длиной.
5.
Обдумать как достучаться до конкретного адреса (байта) при побайтовой адресации, используя byteenable и транслировать в слова или как?
6.
По байтам дублировать удобнее, но меньше вариация паттернов, так как повторяются. Можно и на каждый байт свой РСЛОС, громоздко в плане кода, но при разной инициализации все байты слова будут рандомные.
7.

8.

9.
Программа задающая тест должна проверять валиден ли такой тест-кейс или нет, если нет - сообщать об этом пользователю в виде окна с ошибкой. (Например, длину берста, допустима ли она при адресации по словам). Все ли параметры заданы, не были ли забыты какие-то необходимые для теста.
